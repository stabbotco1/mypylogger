# Additional Steering Directives for mypylogger

These directives take precedence when Kiro builds out this project.

1. Code Integrity
   - The code must always run after every change.  
   - No breaking changes should ever be submitted.  

2. Environment
   - Use a local `venv` to manage Python version and dependencies.  

3. Development Approach
   - Follow test-driven development (TDD).  
   - Each new unit of code must include or pass tests to ensure no regressions.  

4. Task Planning
   - Generate tasks in correct order to avoid referencing modules not yet implemented.  
   - Maintain incremental progress with functional, executable code at all stages.  

5. Documentation
   - Maintain a Markdown file documenting module dependencies as a graph.  

6. Dependencies
   - Avoid non-standard Python libraries unless explicitly suggested and approved.  

7. Project Context
   - Project name: `mypylogger`.  
   - `.kiro` directory is at the project root — verify before work begins.  
   - GitHub repository: `https://github.com/stabbotco1/mypylogger`.  
   - Local checkout: `/Users/stephenabbot/projects/mypylogger`.  
   - Ensure Kiro’s internal references match both repo and local paths.  

8. Priorities
   - Deliver code concisely, in logical order, and incrementally.  
   - Ensure continuous execution and a well-structured codebase.  

9. Imports
   - Document all root-level Python imports in a project artifact (e.g., root `README.md`).  
   - Only base-level dependencies should be listed, not inherited transitive imports.  
