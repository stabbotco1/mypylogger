retest github runners with dependencies
retest1

## GitHub Actions Pipeline Fix

**Issue**: Upload Coverage job failing with "Input required and not supplied: aws-region"

**Root Cause**: The `upload-coverage` job was missing the `aws-region` parameter in the AWS credentials configuration step, while other jobs (deploy, notify-status) had it correctly configured.

**Fix Applied**: Added missing `aws-region: ${{ vars.AWS_REGION }}` parameter to the `Configure AWS credentials via OIDC` step in the `upload-coverage` job.

**Status**: ✅ Fixed - Pipeline should now pass the AWS credentials configuration step

## Actions Taken:
1. ✅ Identified missing `aws-region` parameter in upload-coverage job
2. ✅ Added `aws-region: ${{ vars.AWS_REGION }}` to AWS credentials configuration
3. ✅ Validated YAML syntax is correct
4. ✅ Committed fix with message: "fix: add missing aws-region parameter to upload-coverage job"
5. ✅ Pushed changes to pre-release branch

## Next Steps:
- Monitor GitHub Actions pipeline run to verify the fix resolves the "Upload Coverage" job failure
- Check that all jobs now pass: Quality Gates, Test Matrix, Upload Coverage, Build Package
- The pipeline should now successfully configure AWS credentials and upload coverage to Codecov
test
