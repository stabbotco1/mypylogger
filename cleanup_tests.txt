Command: python -c "
# Quick cleanup of test file imports
import re, os
test_files = ['tests/test_cache_manager.py', 'tests/test_documentation_validation.py', 'tests/test_end_to_end_integration.py']
for f in test_files:
    if os.path.exists(f):
        with open(f, 'r') as file: content = file.read()
        content = re.sub(r'^from unittest.mock import Mock, patch$', 'from unittest.mock import patch', content, flags=re.MULTILINE)
        content = re.sub(r'^from typing import Dict, List, Optional, Tuple$', '', content, flags=re.MULTILINE)
        with open(f, 'w') as file: file.write(content)
        print(f'Cleaned {f}')
"
